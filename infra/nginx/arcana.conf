# arcana.conf — the only door into the platform from outside the host.
#
# Every service binds to 127.0.0.1 and nothing else (see each main.ts). That is
# deliberate and stays true: this file is what decides which of their routes the
# outside world can reach, and it is a short list.
#
# TLS IS NOT HERE YET. The listen directive below is loopback-only on purpose —
# the domain has not been chosen, and a certificate cannot be issued without one.
# Going public is a one-line change to `listen`, plus the TLS block, and it is a
# decision rather than a default: this host holds funded wallets.
#
# ------------------------------------------------------------------ rate limit
#
# TWO LAYERS, AND THEY MEASURE DIFFERENT THINGS. This zone is a coarse net
# against floods, keyed on the real client address. The precise limits — 10
# agent creations an hour per WALLET, and the rest — live in the application,
# where the identity is proven rather than inferred from a packet.
#
# $binary_remote_addr, not a forwarded header: at this layer the peer IS the
# client, and keying on anything the caller can write would let one attacker hold
# as many buckets as they like.
limit_req_zone $binary_remote_addr zone=arcana_api:10m rate=20r/s;
limit_req_status 429;
limit_req_log_level warn;

server {
    # LOOPBACK ONLY UNTIL A DOMAIN IS DECIDED. Replace with `listen 443 ssl;`
    # plus the certificate block, and add `listen 80;` redirecting, when it is.
    listen 127.0.0.1:8080;
    server_name _;

    # The upstreams are node processes, not a CDN. Keep bodies small: nothing
    # public takes an upload, and the largest legitimate POST is a SIWE
    # signature.
    client_max_body_size 64k;
    client_body_timeout 10s;

    # Say nothing about what is running. The version banner is free
    # reconnaissance and buys nobody anything.
    server_tokens off;

    # ------------------------------------------------------------ the API tier
    #
    # THE HEADER IS APPENDED, NOT SET, and that is the whole defence.
    # $proxy_add_x_forwarded_for puts the real peer to the RIGHT of whatever the
    # caller sent, and Express — told to trust only 'loopback' — walks in from
    # the right and stops at the first address this proxy did not vouch for. A
    # caller who forges `X-Forwarded-For: 1.2.3.4` pushes their own value to the
    # left, where it is read as an earlier hop and ignored.
    #
    # Setting it instead of appending, or telling Express `trust proxy: true`,
    # would hand every caller the ability to choose their own rate-limit bucket.
    location /v1/ {
        limit_req zone=arcana_api burst=40 nodelay;

        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header Host $host;

        # The application decides who may call what. The proxy must not be able
        # to grant machine-tier access by accident, so it never forwards a key
        # it was given and never adds one.
        proxy_set_header X-Internal-Key "";

        proxy_http_version 1.1;
        proxy_read_timeout 30s;
        proxy_connect_timeout 5s;

        # Route by prefix. Each service owns its own paths and there is no
        # overlap; anything unlisted falls through to the agent-service default
        # below, which answers 404 for routes it does not have.
        proxy_pass http://127.0.0.1:3001;
    }

    location /v1/marketplace/ {
        limit_req zone=arcana_api burst=40 nodelay;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Host $host;
        proxy_set_header X-Internal-Key "";
        proxy_http_version 1.1;
        proxy_pass http://127.0.0.1:3002;
    }

    location /v1/arca/ {
        limit_req zone=arcana_api burst=40 nodelay;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Host $host;
        proxy_set_header X-Internal-Key "";
        proxy_http_version 1.1;
        proxy_pass http://127.0.0.1:3003;
    }

    location /v1/subscriptions/ {
        limit_req zone=arcana_api burst=40 nodelay;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Host $host;
        proxy_set_header X-Internal-Key "";
        proxy_http_version 1.1;
        proxy_pass http://127.0.0.1:3003;
    }

    # ------------------------------------------------------- the machine tier
    #
    # NEVER REACHABLE FROM OUTSIDE. /internal/* is the tier behind
    # X-Internal-Key: signer wallet export, manual decisions, payment claims,
    # DNA recompute. Those services already bind to loopback, so this is the
    # second lock rather than the only one — and it is here so that a future
    # `proxy_pass` added carelessly cannot open the tier by widening a prefix.
    #
    # 404 and not 403: a refusal that confirms the path exists is an invitation.
    location /internal/ {
        access_log /var/log/nginx/arcana-internal-attempt.log;
        return 404;
    }

    # Health probes answer without touching the API rate limit, so a monitor
    # cannot starve real callers and a flood cannot hide behind /healthz.
    location = /healthz {
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_pass http://127.0.0.1:3001/healthz;
    }

    # ---------------------------------------------------------------- the app
    #
    # The Next.js surface is stage 2. Until it exists this answers 503 rather
    # than a holding page: a page that renders with nothing behind it is exactly
    # the "empty value that reads like data" this project keeps removing, and a
    # monitor should see the difference.
    location / {
        return 503 "arcana web is not deployed yet\n";
        add_header Content-Type text/plain always;
    }
}
